package com.jatesun.week2;

/**
 * @author sunjiantao
 * @date 2019-11-03
 */
public class NumberOfSubarrays {
    /*
    思路：
    1、以k为最小数组,一直循环到只能原数组唯一数组，返回结果
     */
    public int numberOfSubarrays(int[] nums, int k) {
        if (k > nums.length) {
            return 0;
        }
        int source = k;
//        while (source)
//            for ()
        return 0;
    }
}
